extends layouts/default

block content
	include ./hero
	include ./filter

	.IconsWrapper.ui.container
		each icon in icons
			.Item
				.Item__content
					.Item__icon
						!= icon.icon
					.Item__name
						p= icon.name